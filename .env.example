# Required for Vercel KV storage (auto-injected by Vercel):
KV_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# JWT secret (change in production):
JWT_SECRET=your-random-secret-here

# Port (local dev only, default 8090):
PORT=8090
